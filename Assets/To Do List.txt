Geral:
 - Check the necessity for inheritance for units (selection, movement, HUD display, etc...);

Camera:
 - Change from click-based to isometric camera; - Ok

AI:
 - Create enemy's path finding;
 - Create enemies' life bars;

Gameplay:
 - Create the lives system;
 - Create victory/loss conditions;
 - Create more waves with enemies;

Player:
 - Fix the movement animation direction;

Interface:
 - Create an interface;
 - Display enemy health bars on top of their heads;
 - Change HUD components;
 - Retractable HUD (Save in player prefs);

Map:
 - Beautify the map;
 - Add random static meshes;
 - Improve the terrain;